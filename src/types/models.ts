export interface Team {
    score: number
    good_answers: number
    title_validated: boolean
    singer_validated: boolean
}

export interface Song {
    id: number
    title: string
    author: string
    year: string
}